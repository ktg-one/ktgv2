---
milestone: v1.1
name: Hub production evidence, polish, merge
audited: 2026-04-01
status: gaps_found
source_phases_verified: 0
source_phases_expected: hub roadmap phases 1–7 + marketing plans
scores:
  requirements_traceability: 0/15
  phase_verification_files: 0/unknown
  integration_e2e: not_recorded
  external_audits:
    nyquist: partial
    ui_six_pillar: flag_block_mixed
notes:
  - No `.planning/phases/*/*-VERIFICATION.md` files exist; phase-level GSD verification was never written. This audit aggregates external reviewer outputs + planning truth.
gaps:
  planning:
    - All REQ IDs in REQUIREMENTS.md traceability remain unchecked Pending — close when marketing/hub phases complete or retarget traceability to hub milestones.
  verification:
    - Phase 6 (hub): recorded smoke evidence on Vercel (preview/prod) still required for Nyquist-style closure.
    - Phase 1 marketing (01-02): open QA per ROADMAP / 01-02-PLAN.
---

# Milestone v1.1 — audit report

## Executive summary

**Verdict:** `gaps_found` — milestone **not** closed to a Nyquist-complete bar: live verification evidence is incomplete, requirement checkboxes are stale, and formal phase VERIFICATION artifacts are missing. **External** `gsd-ui-auditor` and `gsd-nyquist-auditor` runs added actionable items below (user-requested consolidation).

---

## A. Nyquist-oriented action items (`gsd-nyquist-auditor`)

| # | Item | Owner | Evidence when done |
|---|------|--------|-------------------|
| N1 | **Record Phase 6 smoke** on **Vercel** (and/or local): send message, streaming, model switch, personas — match checklist in `STATE.md` | Kevin / deploy | Dated note in `STATE.md` or checklist file with URLs (preview/prod) |
| N2 | **Marketing track:** close or explicitly defer **01-02** shell/motion QA per `.planning/phases/01-marketing-shell/01-02-PLAN.md` | Kevin | STATE / ROADMAP updated |
| N3 | **REQUIREMENTS.md:** reconcile traceability table with reality — check off satisfied v1 reqs or remap phases; or add **HUB-*** REQ IDs for chat | Planning | Traceability table shows `[x]` where verified |
| N4 | **Zod / install reproducibility:** confirm `npm ci` (or documented install) matches CI before calling hub “production-proof” | Eng | CI green + note in STATE if blocker cleared |
| N5 | **Automated checks (backlog):** no test suite today; optional minimal smoke (build + one API integration) | Eng | Issue or Phase 7 scope |
| N6 | **Honest product copy:** MCP toggles beyond documented stub = UI-only until real transport; keep `STATE`/`openmemory` aligned | Docs | No “fully wired MCP” claim without servers |
| N7 | **POST `/api/hub/chat`:** validate streaming on **Node** runtime after deploy (regression vs edge) | Kevin | Network trace or log snippet archived |

---

## B. UI review action items — six-pillar (`gsd-ui-auditor`) + shadcn lens

| # | Item | Priority | Notes |
|---|------|----------|--------|
| U1 | **Theme tokens vs hex** — reduce ad hoc `#00f0ff`; map AI accent to a semantic CSS variable in `@theme` / `globals.css` | FLAG | Aligns with shadcn “surfaces from tokens” |
| U2 | **Accessibility — switches** — ensure **Settings** (and any rail) `Switch` rows have **`aria-label`** (or visible label + `htmlFor`) consistent with connections sidebar | FLAG | |
| U3 | **Inject UI controls** — inject “dots” / toggles: sufficient target size, **`aria-pressed`** or checkbox pattern, don’t rely on colour alone for state | FLAG | |
| U4 | **`prefers-reduced-motion`** — respect for hub chat motion / transitions where added | FLAG | |
| U5 | **shadcn composition** — replace raw **`input` / `textarea`** in modals with **`Input` / `Textarea`** where appropriate | Nice | |
| U6 | **Destructive actions** — preset/macro **delete** (or similar): prefer **`AlertDialog`** over plain **`Dialog`** | BLOCK-adjacent | shadcn recipe |
| U7 | **Single vs dual mode** — second `useChat` must either **drive visible dual-pane UI** when `isDualMode` **or** stay unmounted in single mode; see todo `2026-04-01-document-hub-chat-single-vs-dual-playground-mode.md` | BLOCK-adjacent | Playground parity |
| U8 | **Remove dead imports / dead state** after dual is wired — pass lint, no unused hooks | Hygiene | |

---

## C. Phase verification files (GSD)

| Phase / plan area | VERIFICATION.md | Status |
|-------------------|-----------------|--------|
| `01-marketing-shell` | Missing | Unverified |
| `hub-chat` (plans) | Missing | Unverified |

**Recommendation:** add `*-VERIFICATION.md` when closing Phase 6 and Phase 7, or run `$gsd-validate-phase` Nyquist pass where applicable.

---

## D. Requirements coverage (traceability snapshot)

`REQUIREMENTS.md` lists **15** v1 requirements, all traceability rows **Pending**. Milestone v1.1 is **hub-heavy**; marketing REQ IDs may be partially satisfied in code without checkbox updates.

**Action:** one planning pass to either (1) verify and check boxes, or (2) split hub requirements into explicit REQ IDs mapped to phases 5–7.

---

## E. Integration / E2E (placeholder)

**Not executed in this audit** (no `gsd-integration-checker` spawn): no phase SUMMARY/VERIFICATION corpus. When Phase 6 evidence exists, add:

- Marketing: `/` → blog → hub entry
- Hub: `/hub/chat` → message → stream → model change → optional skills toggle

---

## Next steps (routing)

1. Close **N1 + N7** before claiming hub verified.  
2. Triage **U6 + U7** for Phase 7 or v1.2 hub parity.  
3. Batch **U1–U5** as polish.  
4. `$gsd-plan-milestone-gaps` if you want formal gap phases, or update `STATE.md` as each item completes.

---

*Generated: 2026-04-01 — `$gsd-audit-milestone` lite + user-requested auditor action items.*
